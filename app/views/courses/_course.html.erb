<p><%= link_to course.name, course_path(course) %> </p>

<%= link_to 'editar', edit_course_path(course) %>
<%= link_to 'deletar', course_path(course), method: :delete, data: { confirm: 'Tem certeza que deseja apagar esse Curso?' }, class: 'delete' %>